#include <iostream>
#include "RD9.h"

/*
1. Користувач зчитує з клавіатури масив, розмір 10. Написати функцію, яка додатні елементи масиву множить на 2, від’ємні робить 0. void translateArray(int numbers[], int size); Вивести масив у консоль.

2. Зчитати рядок з консолі. Функція, яка перетворює усі символи латиниці у верхній регістр (без використання std функцій); void toUppercase(char str[]);

Input: aBBcce%.-ab

Output: ABBCCE%.-AB

3. Функція, яка перевіряє чи введений рядок паліндром (Паліндром -  слово, число, набір символів, що однаково читається в обох напрямках: зліва направо та справа наліво)

bool isPalindrom(const char str[]);

abc -> true ; tenet -> true ; abbc -> true ; teneT -> false ; abcd -> false ; abca -> false

4. Функція, яка підраховує кількість голосних та приголосних у рядку:

void parseStringLetters(const char[] str, int& vowelsCount, int& constonantsCount);

5. Функція для порівняння двох рядків без використання вбудованих бібліотек:

bool isEqual(const char str1[], const char str2[]);
*/


int main()
{
    task5();
}